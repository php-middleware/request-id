checks:
    php: true
build:
    environment:
        php:
            version: 5.6.16
    tests:
        override:
            -
                command: 'phpunit --coverage-clover=tmp/phpunit-coverage-clover.xml'
                coverage:
                    file: 'tmp/phpunit-coverage-clover.xml'
                    format: 'php-clover'
